{
  "name": "udistricts",
  "version": "2.1.0",
  "description": "A package to install ugandan districts",
  "main": "index.js",
  "scripts": {
    "test": "nyc mocha --timeout 5000",
    "coverage": "./node_modules/istanbul/lib/cli.js cover ./node_modules/mocha/bin/_mocha -- --ui bdd -R spec -t 5000",
    "coveralls": "mocha --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0",
    "cherio": "^1.0.0-rc.2",
    "istanbul": "^0.4.5",
    "nyc": "^14.1.1",
    "request": "^2.88.0",
    "superagent": "^5.1.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "coveralls": "^3.0.7",
    "mocha": "^6.2.2"
  }
}
